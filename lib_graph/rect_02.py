from graph import *

penColor(0, 0, 0)  # цвет обводки
penSize(5)  # изменяет размер обводки ( 1 значение = 1 пиксель)
brushColor("brown")  # цвет заливки
rectangle(10, 20, 50, 60)  # фигура и ее параметры (x1, y1, x2, y2)

run()  # вызов окна, чтобы видеть фигру (открывает окно с получившимся кодом
